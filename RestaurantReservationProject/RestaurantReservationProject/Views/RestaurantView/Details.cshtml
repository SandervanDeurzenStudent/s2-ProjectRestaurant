@model Presentation.Models.IndexViewModel

@{
    ViewData["Title"] = "Details";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Details</h2>

<div>
    <h4>RestaurantModel</h4>
    <hr />
    <dl class="dl-horizontal">
        @foreach (var item in Model.commentList)
        {
            <dt>
                @Html.DisplayNameFor(model => item.Name)
            </dt>
            <dd>
                @Html.DisplayFor(model => item.Name)
            </dd>
            <dt>
                @Html.DisplayNameFor(model => item.Name)
            </dt>
            <dd>
                @Html.DisplayFor(model => item.Info)
            </dd>
            <dt>
                @Html.DisplayNameFor(model => item.Name)
            </dt>
        }

    </dl>
</div>

<div>
    <a asp-action="Edit" asp-route-id="@Model.restaurantModel.Name">Edit</a> |
    <a asp-action="Index">Back to List</a>  |
    <a asp-controller="CommentView" asp-action="Create" asp-route-id="@Model.restaurantModel.Id">Make Comment</a>
</div>
